%\voffset=0pt
%\hoffset=0pt
%\setlength\topmargin	{-0,5in}
%\setlength\headheight	{10pt}
%\setlength\headsep		{0.25in}
%\setlength\footskip		{0.75in}
%\setlength\textwidth	{\paperwidth}
%\addtolength\textwidth	{-2in}
%\setlength\textheight	{\paperheight}
%\addtolength\textheight	{-2in}
%\setlength\oddsidemargin{0in}
%\setlength\evensidemargin{0in}
%\setlength\parindent	{0pt}
%\setlength\parskip		{10pt plus 5pt minus 5pt}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%Cambiamos el estilo de myheadings
%\addto\captionsspanish{\renewcommand\chaptername{Práctica}}
%\renewcommand{\ps@myheadings}{%
%			  \renewcommand{\@evenhead}{%
%			  				\hss\vbox{%
%			  				\emph{Escuela superior de Ingeniería\hfill
%			  				ITIS}}}
%			  
%			  \renewcommand{\@oddhead}{%
%			  				\hss\vbox{%
%			  				\emph{Automatización de las tareas de  mantenimiento de equipos informáticos mediante BASH.\hfill
%			  				IMEI}}}
%			  
%			  \renewcommand{\@oddfoot}{\hfill\normalsize\rm\thepage\hfill}
%			  \renewcommand{\@evenfoot}{{\hfill\normalsize\rm\thepage\hfill}}}
%			  \renewcommand\sin{\mathop{\operator@font sen}\nolimits}
%			  \renewcommand\arcsin{\mathop{\operator@font arcsen}\nolimits}
%	  
%\pagestyle{myheadings}

%Codigo en C
    \definecolor{gray97}{gray}{.97} %Definimos los colores a usar.
    \definecolor{gray75}{gray}{.75}
    \definecolor{gray45}{gray}{.45}
    \usepackage{listings}
    %Comando general para editar los parametros del ambiente listing.
    \lstset{	
     %Opciones de enmarcación.					
     frame=Ltb,						%Estilo del marco (hay muchas opciónes como: frame=trBL, frame=shadowbox...
     %frameround=fttt				%Esta opción recibe 4 caracteres, que corresponden a las 4 esquinas, comenzando por la superior derecha y siguiendo en el sentido del reloj. La f indica esquina en punta, las t redondeada.
    framerule=0pt,					%Ancho del marco
    aboveskip=0.5cm,				%Espacio vertical
    framextopmargin=3pt,			%Margenes del marco (superior,
    framexbottommargin=3pt,			%inferior,
    framexleftmargin=0.4cm,			%e izquierdo)
    framesep=0pt,
    rulesep=.4pt,
    backgroundcolor=\color{gray97},	%Color del fondo de la caja
    rulesepcolor=\color{black},	
    %Opciones de las fuentes empleadas.
    stringstyle=\ttfamily,			%Estilo empleado en cadenas de caracteres.
    showstringspaces = false,
    basicstyle=\scriptsize\ttfamily,		%Estilo general
    commentstyle=\color{gray45},	%Estilo de los comentarios
    keywordstyle=\bfseries,			%Estilod e las palabras reservadas.
    %Configuramos los números de linea:
    numbers=left,					%Posición
    numbersep=15pt,					%Separación
    numberstyle=\tiny,				%Tamaño
    numberfirstline = false,		%
    breaklines=true,				%
    firstnumber=1					%Indicar el primer número de linea (no es necesario en este caso)
    %stepnumber=n					para saltarse una linea cada n. Yo no lo utilizo en este ejemplo.
    }

    % minimizar fragmentado de listados
    \lstnewenvironment{listing}[1][]%Definimos un nuevo entorno para evitarnos repetir lo mismo en cada ocasión. Indicamos además, que recibe un argumento.
    {\lstset{#1}\pagebreak[0]}{\pagebreak[0]}%El valor del argumento que recibe lo utiliza en \lstset

    \lstdefinestyle{consola}		%Definimos un estilo en particular para consola, en el que moodificamos algunos parámetros.
    {basicstyle=\scriptsize\bf\ttfamily,
    backgroundcolor=\color{gray75},
    }

    \lstdefinestyle{C}			%Definimos un estilo para C (en este caso usamos los parametros por defecto que indicamos al inicio, pero indicando el lenguaje para que resalte correctamente la sintaxis).
    {language=C,
    }


